// Middleware visto en clase adicional, para validar un post

/*
function auth (req, res, next) {
    const token = req.token
    if (!token) {
        res.send({error: "Necesita autenticarse"})
    } else {
        req.token=token
        next()
    }
}
*/